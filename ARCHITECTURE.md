SearchRules odpowiadają za przechowywanie słów kluczowych używanych do wyszukiwania ofert pracy.

Na początku implementacja używa danych w pamięci aplikacji, aby najpierw zbudować i zrozumieć warstwę HTTP oraz logikę domenową. Docelowo dane zostaną przeniesione do ScyllaDB.
